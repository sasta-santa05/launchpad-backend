# Server
server.port=8080

# MongoDB — replace YOUR_PASSWORD and YOUR_CLUSTER with your actual values
spring.data.mongodb.uri=mongodb+srv://adivpatil565_db_user:Aditya123@launchpad.5sdfwlw.mongodb.net/?appName=launchpad
spring.data.mongodb.database=launchpad

# JWT
jwt.secret=launchpad_super_secret_key_2024_minimum_256_bits_long_enough
jwt.expiration=86400000

# CORS
cors.allowed-origins=http://localhost:3000

# Multipart uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Show SQL-like logs for MongoDB (optional - remove in production)
logging.level.org.springframework.data.mongodb=DEBUG
